## 1. Remaining staff pages

- [ ] 1.1 Class session create/list and booking list — verify: session appears; book from API still reflected
- [ ] 1.2 Trainer assign/unassign UI — verify: trainer then sees client via API
- [ ] 1.3 Reports dashboard for owner/manager; hidden/403 for receptionist — verify: both roles

## 2. Integration verification

- [ ] 2.1 Core loop still works (member, pay, check-in) after new nav — verify: click-through
- [ ] 2.2 `npm run build` in `admin/` succeeds — verify: `pnpm --filter admin build` exit 0
